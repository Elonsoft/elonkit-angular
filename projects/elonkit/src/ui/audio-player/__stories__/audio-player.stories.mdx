import { Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import { Canvas } from '~storybook/components';
import { action } from '@storybook/addon-actions';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';

import { AUDIO_PLAYER_STORY_BASIC_SOURCE } from './audio-player-story-basic';

import {
  AUDIO_PLAYER_STORY_LOCALE_SOURCE,
  AudioPlayerStoryLocaleComponent,
  AudioPlayerStoryLocaleModule
} from './audio-player-story-locale';

import { ESAudioPlayerModule, ESAudioPlayerComponent } from '..';

<Meta title='UI/Audio Player' />

# Audio player

Audio player allows users to play audio tracks.

## Demos

<Canvas source={AUDIO_PLAYER_STORY_BASIC_SOURCE}>
  <Story name='Basic' height='170px'>
    {((args, context) => ({
      template: `<es-audio-player source='https://ia800905.us.archive.org/19/items/FREE_background_music_dhalius/backsound.mp3' volume="100"></es-audio-player>`,
      moduleMetadata: {
        imports: [BrowserAnimationsModule, ESAudioPlayerModule]
      },
      props: {
        ...args
      }
    })).bind({})}
  </Story>
</Canvas>

We can override default locale which is used for menu items.

<Canvas source={AUDIO_PLAYER_STORY_LOCALE_SOURCE}>
  <Story name='Locale' height='110px'>
    {((args, context) => ({
      component: AudioPlayerStoryLocaleComponent,
      moduleMetadata: {
        imports: [BrowserAnimationsModule, AudioPlayerStoryLocaleModule]
      },
      props: {
        ...args
      }
    })).bind({})}
  </Story>
</Canvas>

## API

<ArgsTable of={ESAudioPlayerComponent} />
