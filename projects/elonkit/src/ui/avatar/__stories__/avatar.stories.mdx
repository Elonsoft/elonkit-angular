import { Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import { Canvas } from '~storybook/components';
import { action } from '@storybook/addon-actions';

import { MatIconModule } from '@angular/material/icon';

import { ESAvatarComponent, ESAvatarModule } from '..';

import { AVATAR_STORY_IMAGE_SOURCE } from './avatar-story-image';

import { AVATAR_STORY_NO_IMAGE_ICON_SOURCE } from './avatar-story-no-image-icon';

import {
  AvatarStoryNoImageTypographyModule,
  AvatarStoryNoImageTypographyComponent,
  AVATAR_STORY_NO_IMAGE_TYPOGRAPHY_SOURCE
} from './avatar-story-no-image-typography';

import {
  AvatarStoryGroupModule,
  AvatarStoryGroupComponent,
  AVATAR_STORY_GROUP_SOURCE
} from './avatar-story-group';

<Meta title='UI/Avatar' />

# Avatar

This component allows to display default or custom avatar.

## Demos

####

####

### Image avatars

Are created by passing inputs:

####

`src` - path to image;
`alt` - text for alt attribute;

<Canvas source={AVATAR_STORY_IMAGE_SOURCE}>
  <Story
    name='Image'
    args={{
      src: '/img/es-logo.png',
      alt: 'alt text',
      variant: 'round',
      size: 40
    }}
    argTypes={{
      variant: {
        control: { type: 'select', options: ['round', 'square'] }
      }
    }}
  >
    {((args) => ({
      template: `
        <es-avatar
          [size]="size"
          [src]="src"
          [alt]="alt"
          [variant]="variant"
        ></es-avatar>
      `,
      moduleMetadata: {
        imports: [ESAvatarModule]
      },
      props: {
        ...args
      }
    })).bind({})}
  </Story>
</Canvas>

### Icon avatars

Are created by passing mat-icon via ng-content instead of src input

<Canvas source={AVATAR_STORY_NO_IMAGE_ICON_SOURCE}>
  <Story
    name='Icon'
    args={{
      variant: 'round',
      size: 40
    }}
    argTypes={{
      variant: {
        control: { type: 'select', options: ['round', 'square'] }
      }
    }}
  >
    {((args) => ({
      template: `
        <es-avatar
          [size]="size"
          [variant]="variant"
        >
          <mat-icon>thumb_up</mat-icon>
        </es-avatar>
      `,
      moduleMetadata: {
        imports: [ESAvatarModule, MatIconModule]
      },
      props: {
        ...args
      }
    })).bind({})}
  </Story>
</Canvas>

### Text avatars

Are created by passing typography via ng-content instead of src input.

####

`textTypography` - css class for passing text;

<Canvas source={AVATAR_STORY_NO_IMAGE_TYPOGRAPHY_SOURCE}>
  <Story
    name='Text'
    args={{
      variant: 'round',
      size: 40,
      textTypography: 'typography'
    }}
    argTypes={{
      variant: {
        control: { type: 'select', options: ['round', 'square'] }
      }
    }}
  >
    {((args) => ({
      component: AvatarStoryNoImageTypographyComponent,
      moduleMetadata: {
        imports: [AvatarStoryNoImageTypographyModule]
      },
      props: {
        ...args
      }
    })).bind({})}
  </Story>
</Canvas>

### Group avatars

Are created by looping of avatars components

####

`size` - size of all avatars in group;

<Canvas source={AVATAR_STORY_GROUP_SOURCE}>
  <Story
    name='Group'
    args={{
      size: 40
    }}
  >
    {((args) => ({
      component: AvatarStoryGroupComponent,
      moduleMetadata: {
        imports: [AvatarStoryGroupModule]
      },
      props: {
        ...args
      }
    })).bind({})}
  </Story>
</Canvas>

## API

<ArgsTable of={ESAvatarComponent} />

## Interfaces

ESAvatarDefaultOptions ESAvatarVariant defines default avatar form

```ts
export interface ESAvatarDefaultOptions {
  size?: number;
  textTypography?: string;
  variant?: ESAvatarVariant;
}
```

```ts
export enum ESAvatarVariant {
  Round = 'round',
  Square = 'square'
}
```

Injection token that can be used to configure the default options for all components within an app.

```ts
import { ES_AVATAR_DEFAULT_OPTIONS } from '@elonsoft/elonkit/ui/avatar';

@NgModule({
  providers: [
    {
      provide: ES_AVATAR_DEFAULT_OPTIONS,
      useValue: {
        size: 40,
        variant: 'round'
      }
    }
  ]
})
```
